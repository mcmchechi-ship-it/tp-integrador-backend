# Copia este archivo como .env y ajusta los valores
PORT=3000

# PostgreSQL (usa la contraseña que definiste al instalarlo)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tp_integrador
DB_USER=postgres
DB_PASSWORD=tu_contraseña_aquí
# Poner en true para ver en consola cada consulta SQL
DB_LOGGING=false
